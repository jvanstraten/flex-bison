"""This is just a wrapper of GNU Flex and Bison in Python.

The purpose is to make flex/bison available when needed as a prerequisite for
building documentation on ReadTheDocs. ReadTheDocs doesn't provide flex/bison
and can only pull in Python dependencies, so here we are."""
